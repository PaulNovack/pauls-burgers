python -m venv .venv      # create local venv folder
source .venv/bin/activate # activate (you’ll do this per-shell)
python -m pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
